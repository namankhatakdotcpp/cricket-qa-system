# ── HuggingFace Space requirements (deterministic path only) ─────────────────
# Heavy ML deps (torch, transformers, peft, trl) are excluded.
# The /infer endpoint works perfectly with use_llm=false (the default).
# Requests with use_llm=true will return HTTP 503 gracefully.

fastapi==0.115.4
uvicorn[standard]==0.32.1
pydantic==2.9.2
python-multipart==0.0.12
slowapi==0.1.9

# ── Data ─────────────────────────────────────────────────────────────────────
pandas==2.3.0
pyarrow==21.0.0
duckdb==1.3.0

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0
